<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpotifyDownloader.Views.Shared.LoadingIndicator"
               IsVisible="{Binding ShowLoading}" VerticalOptions="Center" HorizontalOptions="Center" HeightRequest="100" WidthRequest="100" Background="#80FFFFFF">
    <ActivityIndicator IsRunning="{Binding ShowLoading}"/>
    <Label Text="Loading..."/>
</StackLayout>