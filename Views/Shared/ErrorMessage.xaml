<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SpotifyDownloader.Views.Shared.ErrorMessage"
             IsVisible="{Binding HasError}" BackgroundColor="Red" Padding="5">
    <Label Text="{Binding ErrorMessage}" TextColor="White" FontSize="20"/>
</StackLayout >